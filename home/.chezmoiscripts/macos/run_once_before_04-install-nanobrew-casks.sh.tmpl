#!/usr/bin/env bash
{{/* Casks are installed through nanobrew after the nb binary exists. */ -}}
{{ if and (eq .chezmoi.os "darwin") (eq .chezmoi.arch "arm64") -}}
DOTFILES_NANOBREW_CASKS_BUNDLE_CONTENT="$(cat <<'DOTFILES_NANOBREW_CASKS_EOF'
{{ include "../install/macos/common/nanobrew-casks.Brewfile" }}
DOTFILES_NANOBREW_CASKS_EOF
)"
export DOTFILES_NANOBREW_CASKS_BUNDLE_CONTENT

{{   include "../install/macos/common/nanobrew-casks.sh" }}
{{- end }}
